# Tracked deployment configuration. Non-secret values only.
# Credentials (R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, ONLYOFFICE_JWT_SECRET, POSTGRES_URL)
# come from Cloudflare Worker secrets, or from a local untracked config.properties.
# Precedence: environment variables > config.properties > this file.

R2_ENABLED=true
R2_ACCOUNT_ID=f6bec29ca27c7c76e8e4b3f0fc23d0c2
R2_BUCKET_NAME=nbah-repo
R2_TEMPLATE_PREFIX=Templates/
R2_CLIENT_FOLDER=Clients/

# Persistence driver: json (output/*.json files) or postgres.
DATA_STORE=json
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DATABASE=nabh_docs
POSTGRES_USER=postgres
# true | no-verify | false
POSTGRES_SSL=false
